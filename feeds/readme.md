# Feeds

This modules parses external data and converts it into nicer types and data structures, and fixes names when the underlying source is ambiguous.

Business/display logic, (e.g. computing the low temperature for rest of day) is done by downstream consumers.
